interface:
  display_name: "Blog"
  short_description: "Repo-specific patterns and workflows for blog"
  default_prompt: "Use the blog repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true