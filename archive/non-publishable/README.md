# Non-publishable content

This directory preserves incomplete pages and internal rendering fixtures that must not
become part of the public Hugo site.

Everything under `content/` is treated as publishable. Move a file there only after its
content and language version are ready for release.
