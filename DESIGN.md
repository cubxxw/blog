# Blog Design Direction

> 本文档定义博客的项目级视觉方向与前端设计约束。它将 Taste Skill
> 的通用方法转译为适合当前 Hugo、PaperMod、双语长文博客的规则。
> 本文档不替代 `CLAUDE.md` 中的内容、Front Matter、标签和发布规范。

## Design Read

这是一个面向技术读者与个人成长读者的双语个人出版物。视觉语言应当
兼具 editorial publication 的阅读品质、technical notebook 的清晰结构，
以及作者本人温和、探索性的个人气质。

它不是 SaaS 落地页、个人仪表盘或组件展示场。设计首先服务于阅读、
作者识别和内容发现。

## Design Dials

| Dial | Value | Meaning |
| --- | ---: | --- |
| `DESIGN_VARIANCE` | 6 | 桌面端允许适度非对称，移动端严格回归单栏 |
| `MOTION_INTENSITY` | 3 | 以状态反馈和轻微过渡为主，不做表演性动画 |
| `VISUAL_DENSITY` | 4 | 首页适度收敛，文章正文保持安静和舒展 |

改版模式固定为 `Redesign - Preserve`。除非用户明确要求全面重做，否则：

- 保留 Hugo 和 PaperMod 技术基础
- 保留现有 URL、内容结构和导航语义
- 保留中英文差异化排版
- 保留现有 SEO、结构化数据和分析事件
- 优先精修排版、间距、色彩和交互，不整体换皮

## Visual Foundation

### Technology

- 使用 Hugo 模板和原生 CSS
- 使用 CSS custom properties 维护设计 token
- 不为套用设计范式迁移到 React、Next.js 或 Tailwind
- 交互优先使用 CSS；确需 JavaScript 时保持独立、轻量并可降级
- 滚动感知使用 IntersectionObserver 或 CSS scroll-driven animation
- 禁止用逐帧 scroll 事件更新布局或视觉状态

### Typography

- 英文展示字体可以使用具有出版气质的 serif，但必须服务于作者品牌，
  不能只因为“看起来高级”而使用
- 中文标题和正文继续采用本地字体优先策略，性能与字形稳定性优先
- 正文、标题、元数据和代码必须保持明确的字体职责
- 正文理想行宽为约 60 至 70 个字符
- 中文正文行高可以高于英文，但同一语言内保持稳定节奏
- 不因外部设计规则机械替换已有字体；字体变更必须通过中英文截图对照

### Color

- 每种语言与主题只使用一个主要 accent
- 同一页面不能在不同区块任意切换 accent 色族
- 明暗模式保持相同的信息层级，不允许某个区块突然反转为另一套主题
- 正文、链接、按钮和表单必须满足 WCAG AA 对比度
- 深色模式重点保证长时间阅读舒适度，避免高饱和颜色大面积发光

### Shape

当前圆角系统：

- 小型控件：`--radius-sm` 或 `--radius-md`
- 内容容器：`--radius-lg`
- 主要展示容器：最多使用 `--radius-xl`
- pill 仅用于状态、紧凑筛选或明确的胶囊按钮

禁止把普通导航、元数据、标签、社交入口和装饰文字全部做成 pill。
页面中出现大量 pill 时，应优先使用文本链接、留白、列布局或轻分隔线。

## Content and Layout Rules

### Homepage

首页必须在首屏内回答三个问题：

1. 作者是谁
2. 主要写什么
3. 从哪里开始阅读

首屏优先级：

1. 作者身份与一句核心介绍
2. 一个主要阅读入口
3. 必要的作者视觉
4. 次级导航与更新信息

首页约束：

- 首屏不超过一个主 CTA
- 快捷入口不应与主 CTA 竞争
- 状态、地点、动态和社交信息应当降级，不能全部占据首屏中心
- 不使用提示用户滚动的 scroll cue
- 连续区块不能全部使用相同的白色圆角卡片
- 卡片只在边界、交互或层级确实需要时使用
- 分类入口优先表现为清晰导航，而不是四个同权重功能卡
- 精选或最新文章应成为首页的主要内容主体
- 订阅模块可以有独立视觉身份，但仍应服从全站色彩与圆角系统

### Article Index

- 聚合入口使用 `articles`
- 筛选器和统计信息不能压过文章标题
- 文章卡片重点展示标题、摘要和必要元数据
- 长列表应通过分组、筛选或分页管理，不堆叠重复容器
- 不为每个分类使用互不关联的糖果色体系

### Article Detail

- 保留现有桌面三栏结构，但每个侧栏都必须提供真实阅读价值
- 中间正文是绝对视觉主角
- 文章开头的 description、TL;DR、系列导航、封面和目录不能形成过长的
  “进入正文前流程”
- 左侧 Marginalia 只放稳定元数据
- 右侧 Editorial Tools 只放目录、系列、分享和真正有用的阅读工具
- 小于 1024px 回归单栏；辅助工具进入 Bottom Sheet
- Bottom Sheet 不得遮挡正文关键内容，必须支持键盘、焦点管理和关闭操作
- 代码块、表格、引用、图片和脚注需要统一的阅读语法
- 图片只有在提供信息、证据、解释或叙事价值时使用
- 普通长文不强制配置装饰性配图

### Projects and About

这些页面允许比普通文章更高的视觉表现力，但仍遵循全站 token：

- 项目页可以使用真实截图、封面和成果图
- 禁止用装饰性 div 拼出假的产品截图
- About 页应加强作者识别，不堆砌状态徽章和技能标签
- 图片必须有明确尺寸，避免 CLS

## Motion Rules

每个动画必须至少表达以下一种意义：

- 层级
- 状态变化
- 操作反馈
- 内容进入或退出

如果不能用一句话说明动画传达什么，就删除动画。

具体约束：

- 默认只动画 `transform` 和 `opacity`
- 禁止 `transition: all`
- 禁止通过 React 状态或普通 scroll 监听逐帧更新
- hover transform 只对 `hover: hover` 且 `pointer: fine` 的设备启用
- 所有自动动画都必须支持 `prefers-reduced-motion`
- 文章正文不使用持续循环动画
- 首页最多允许一个环境性持续动画
- press feedback 使用轻微位移或 `scale(0.96-0.98)`
- 主要交互过渡通常不超过 300ms
- 页面进入动画不能让内容在 JavaScript 失败时保持不可见

## Components and Icons

- 优先使用已有图标系统，避免继续增加新的图标家族
- 不手绘复杂 SVG 图标
- emoji 可以作为作者品牌的一部分少量保留，但不能代替一整套界面图标
- 按钮文本在桌面端不得换行
- 所有交互控件必须有 hover、active、focus-visible 和 disabled 状态
- 表单还必须有 loading、success 和 error 状态
- 触控目标建议不小于 44 x 44 CSS pixels

## Copy and Metadata

- 首页和界面文案使用清楚、直接、具体的语言
- 避免泛化营销词和刻意诗化的微型标签
- 不使用无实际意义的版本号、状态点、天气、地点或构建信息装饰页面
- 元数据只展示对当前阅读决策有帮助的内容
- 中文和英文遵循各自正常语法，不让视觉规则干扰正式文章写作
- 正式文章继续遵循 `CLAUDE.md` 中的 Front Matter 和 SEO 规范

## Redesign Priorities

按以下顺序改进，上一层解决后再进入下一层：

1. 排版与文字层级
2. 间距与垂直节奏
3. 色彩和形状一致性
4. 卡片、pill 和装饰元素去噪
5. 交互状态与 reduced motion
6. 首页和关键区块重组
7. CSS 层级与重复规则治理

未经明确批准，不修改：

- URL 和 slug
- 一级导航名称
- Logo 或头像资产
- 法律、隐私和订阅同意文案
- 表单字段名与现有分析事件

## Pre-flight Check

每次视觉修改完成前检查：

- [ ] 修改符合本页 Design Read 和三个 Dial
- [ ] 没有改变 URL、导航、SEO 或内容结构
- [ ] 中英文均已检查
- [ ] 明暗模式均已检查
- [ ] 375px、768px、1024px 和桌面宽度均已检查
- [ ] 页面只有一套 accent 和一致的圆角规则
- [ ] 卡片和 pill 数量有明确必要性
- [ ] 正文仍然是文章页视觉主角
- [ ] 所有动画都有表达目的
- [ ] 自动动画支持 reduced motion
- [ ] hover 效果不会在触屏上形成粘滞状态
- [ ] 键盘焦点清晰可见
- [ ] 文字和按钮对比度达到 WCAG AA
- [ ] 图片尺寸已预留，没有明显 CLS
- [ ] 没有新增不必要的第三方依赖
- [ ] Hugo 生产构建通过
- [ ] Playwright 页面和截图测试通过
- [ ] Lighthouse 关键指标没有明显回退
